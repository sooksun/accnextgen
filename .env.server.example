# ============================================
# ตัวอย่าง Environment สำหรับ Production Server
# คัดลอกเป็น .env บนเซิร์ฟเวอร์ แล้วแก้ค่าให้ตรงกับเซิร์ฟเวอร์
# ============================================

# โหมดรัน
NODE_ENV=production

# Database - MySQL บนเซิร์ฟเวอร์ (แก้ host, user, password, database ให้ตรง)
DATABASE_URL="mysql://USER:PASSWORD@localhost:3306/accnextgen"

# URL แอปที่ผู้ใช้เข้า (ใช้สำหรับลิงก์/redirect)
NEXT_PUBLIC_APP_URL="https://solution-nextgen.co.th"

# โฟลเดอร์เก็บไฟล์อัปโหลด (ใช้ path แบบ absolute บนเซิร์ฟเวอร์)
UPLOAD_DIR="/home/solutio9/accnextgen/uploads"

# PDF (ถ้าใช้ Puppeteer สำหรับพิมพ์ใบกำกับ/ใบเสนอราคา)
# PUPPETEER_EXECUTABLE_PATH="/usr/bin/chromium-browser"
